import { Spinner } from 'flowbite-react';

export default function Loading() {
    // Or a custom loading skeleton component
    return 
    <Spinner color="warning" aria-label="Warning spinner ">
    <p>Loading...</p>
    </Spinner>
  }